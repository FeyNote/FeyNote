# prisma-types

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build prisma-types` to build the library.

## Running unit tests

Run `nx test prisma-types` to execute the unit tests via [Jest](https://jestjs.io).
