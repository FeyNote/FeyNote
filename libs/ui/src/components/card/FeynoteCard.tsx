import styled from 'styled-components';

export const FeynoteCard = styled.div`
  background: var(--card-background);
  border-radius: 4px;
  margin: 8px 8px 0 8px;
  box-shadow: var(--card-box-shadow);
  overflow-y: auto;
`;
