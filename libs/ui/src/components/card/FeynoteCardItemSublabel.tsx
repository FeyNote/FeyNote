import styled from 'styled-components';

export const FeynoteCardItemSublabel = styled.p`
  margin: 0;
  font-size: 0.75rem;
  color: var(--text-color-dim);
  white-space: nowrap;
  text-overflow: ellipsis;
  overflow: hidden;
`;
