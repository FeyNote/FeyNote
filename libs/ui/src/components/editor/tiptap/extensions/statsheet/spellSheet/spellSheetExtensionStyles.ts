import { css } from 'styled-components';
import { statsheetExtensionStyles } from '../statsheetExtensionStyles';

export const spellSheetExtensionStyles = css`
  [data-spellsheet] {
    ${statsheetExtensionStyles}

    font-family: ScalySansRemake;
  }
`;
