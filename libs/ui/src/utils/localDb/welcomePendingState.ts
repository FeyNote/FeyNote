// We don't have a great way to do this between our routing logic and different apps,
// but this works pretty well for now and we don't really expect much from it.
export const welcomePendingSimpleref = {
  welcomePending: false,
};
