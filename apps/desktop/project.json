{
  "name": "desktop",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/desktop/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/desktop && NODE_ENV=development ../../node_modules/.bin/electron-forge start"
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "dependsOn": ["frontend:build"],
      "options": {
        "commands": [
          "rm -rf apps/desktop/renderer && cp -r dist/apps/frontend apps/desktop/renderer",
          "cd apps/desktop && ../../node_modules/.bin/electron-forge package"
        ],
        "parallel": false
      }
    },
    "make": {
      "executor": "nx:run-commands",
      "dependsOn": ["package"],
      "options": {
        "command": "cd apps/desktop && ../../node_modules/.bin/electron-forge make"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json --noEmit",
        "cwd": "{projectRoot}"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
