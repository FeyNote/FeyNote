{
  "name": "www",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/www",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "astro build --root apps/www"
      },
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "astro dev --root apps/www"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
