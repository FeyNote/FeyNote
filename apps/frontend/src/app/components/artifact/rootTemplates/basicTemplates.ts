import { RootTemplate } from './rootTemplates.types';

export const worldRootTemplate = {
  id: 'world',
  title: 'rootTemplate.world.title',
  markdown: 'rootTemplate.world',
  rootTemplate: true,
} satisfies RootTemplate;

export const regionRootTemplate = {
  id: 'region',
  title: 'rootTemplate.region.title',
  markdown: 'rootTemplate.region',
  rootTemplate: true,
} satisfies RootTemplate;

export const genericLocationRootTemplate = {
  id: 'genericLocation',
  title: 'rootTemplate.genericLocation.title',
  markdown: 'rootTemplate.genericLocation',
  rootTemplate: true,
} satisfies RootTemplate;

export const cityTownRootTemplate = {
  id: 'cityTown',
  title: 'rootTemplate.cityTown.title',
  markdown: 'rootTemplate.cityTown',
  rootTemplate: true,
} satisfies RootTemplate;

export const dungeonRootTemplate = {
  id: 'dungeon',
  title: 'rootTemplate.dungeon.title',
  markdown: 'rootTemplate.dungeon',
  rootTemplate: true,
} satisfies RootTemplate;

export const dungeonRoomRootTemplate = {
  id: 'dungeonRoom',
  title: 'rootTemplate.dungeonRoom.title',
  markdown: 'rootTemplate.dungeonRoom',
  rootTemplate: true,
} satisfies RootTemplate;

export const npcRootTemplate = {
  id: 'npc',
  title: 'rootTemplate.npc.title',
  markdown: 'rootTemplate.npc',
  rootTemplate: true,
} satisfies RootTemplate;

export const shopTavernRootTemplate = {
  id: 'shopTavern',
  title: 'rootTemplate.shopTavern.title',
  markdown: 'rootTemplate.shopTavern',
  rootTemplate: true,
} satisfies RootTemplate;

export const playerRootTemplate = {
  id: 'player',
  title: 'rootTemplate.player.title',
  markdown: 'rootTemplate.player',
  rootTemplate: true,
} satisfies RootTemplate;

export const itemRootTemplate = {
  id: 'item',
  title: 'rootTemplate.item.title',
  markdown: 'rootTemplate.item',
  rootTemplate: true,
} satisfies RootTemplate;

export const questRootTemplate = {
  id: 'quest',
  title: 'rootTemplate.quest.title',
  markdown: 'rootTemplate.quest',
  rootTemplate: true,
} satisfies RootTemplate;

export const religionRootTemplate = {
  id: 'religion',
  title: 'rootTemplate.religion.title',
  markdown: 'rootTemplate.religion',
  rootTemplate: true,
} satisfies RootTemplate;
