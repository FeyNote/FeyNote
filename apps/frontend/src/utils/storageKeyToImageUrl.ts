export const storageKeyToImageUrl = (storageKey: string) => {
  // TODO: Change this to an actual asset location, depending on the storage backend implementation details
  return `https://example.com/${storageKey}`;
};
