{
  "name": "queue-worker",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/queue-worker/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "platform": "node",
        "outputPath": "dist/apps/queue-worker",
        "format": ["esm"],
        "bundle": true,
        "main": "apps/queue-worker/src/main.ts",
        "tsConfig": "apps/queue-worker/tsconfig.app.json",
        "assets": ["apps/queue-worker/src/assets"],
        "generatePackageJson": true,
        "esbuildConfig": "apps/queue-worker/esbuild.config.ts"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "queue-worker:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "queue-worker:build:development"
        },
        "production": {
          "buildTarget": "queue-worker:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/queue-worker/jest.config.ts"
      }
    }
  },
  "tags": []
}
