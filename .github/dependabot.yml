version: 2
updates:
  - package-ecosystem: 'npm'
    directory: '/'
    schedule:
      interval: 'weekly'
      day: 'monday'
      time: '16:00' # UTC
    cooldown:
      default-days: 5
      semver-major-days: 10
    open-pull-requests-limit: 50
    groups:
      dedent:
        patterns:
          - 'dedent'
          - '@types/dedent'
      multer:
        patterns:
          - 'multer'
          - '@types/multer'
      tiptapAndHocuspocus:
        patterns:
          - '@tiptap*'
          - '*hocuspocus*'
      eslint:
        patterns:
          - '*eslint*'
      prisma:
        patterns:
          - '@prisma/client'
          - 'prisma'
      jest:
        patterns:
          - 'jest*'
      trpc:
        patterns:
          - '@trpc*'
      socketio:
        patterns:
          - '*socketio*'
      workbox:
        patterns:
          - '*workbox*'
      aws:
        patterns:
          - '@aws-sdk/*'
      sentry:
        patterns:
          - '@sentry/*'
      vite:
        patterns:
          - 'vite'
          - 'vite-*'
          - '@vitejs/*'
      astro:
        patterns:
          - '@astrojs/*'
          - 'astro'
      react:
        patterns:
          - 'react-dom'
          - '@types/react-dom'
          - 'react'
          - '@types/react'
          - 'react-is'
          - '@types/react-is'
      babel:
        patterns:
          - '@babel/*'
      y:
        patterns:
          - 'yjs'
          - 'yjs-*'
          - 'y-*'
      ai-sdk:
        patterns:
          - 'ai'
          - '@ai-sdk/*'
      ionic:
        patterns:
          - '@ionic/*'
          - 'ionicons'
      swc:
        patterns:
          - '@swc*'
      sanitizehtml:
        patterns:
          - 'sanitize-html'
          - '@types/sanitize-html'
      commitlint:
        patterns:
          - '@commitlint/*'
      i18next:
        patterns:
          - 'react-i18next'
          - 'i18next'
    ignore:
      - dependency-name: 'nx'
      - dependency-name: '@nx*' # Ignored because nx must be upgraded through CLI
      - dependency-name: 'tldraw' # Ignored because tldraw must be bumped with service-worker cache
